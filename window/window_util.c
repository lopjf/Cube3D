#include "../cube.h"

int get_rgb(char *rgb)
{
	// int r;
	// int g;
	// int b;
	(void)rgb;

	return (0 << 24 | 0 << 16 | 0 << 8 | 180);
	// return (t << 24 | r << 16 | g << 8 | b);
	// return (create_trgb(0, 0, 0, 180));
}